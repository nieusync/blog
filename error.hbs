{{!< default}}

<section class="hero">
    <div class="container hero-inner">
        <span class="section-label section-label-white">Erro {{statusCode}}</span>
        <div class="accent-line accent-line-white accent-line-center"></div>
        <h1 class="hero-title">{{message}}</h1>
        <p class="hero-subtitle">A página que procura não existe ou foi movida.</p>
    </div>
</section>

<section class="section">
    <div class="container hero-inner">
        <a class="btn btn-gradient" href="{{@site.url}}">Voltar ao blog</a>
    </div>
</section>
